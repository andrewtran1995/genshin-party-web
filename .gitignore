# Build output / generated
.svelte-kit/
build/
dist/
coverage/
src/lib/genshin/data/
static/icons/bosses/
static/icons/elements/
static/icons/weapons/

# Dependencies
node_modules/

# Husky-generated
.husky/_/

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db
*:Zone.Identifier

# Env
.env
.env.*
!.env.example

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
test-results
playwright-report

# Coverage
coverage

# Agent harness directories and runtime files
/.cursor
/.kiro
/.codex
/.gemini
.opencode/settings.local.json
.opencode/scheduled_tasks.lock
.opencode/worktrees/
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/worktrees/
.playwright-mcp
AGENTS.local.md
CLAUDE.local.md
.worktrees/
/worktrees/

# Agent skills: .agents/skills/ is vendored (committed) and is the source of
# truth restored by `pnpm skills:install` per skills-lock.json — see
# scripts/verify-skills-lock.ts. The per-agent copies/symlinks that
# `pnpm skills:sync` fans it out to are regenerated, not committed.
/.claude/skills/**
/.opencode/skills/**
/agent
/agent/**

